---
title: Marquee CTA
description: A dynamic call-to-action component with vertical scrolling marquee effect
---

import { ComponentPreview } from '@/components/ComponentPreview';
import { InstallationTabs } from '@/components/InstallationTabs';
import { PropsTable } from '@/components/PropsTable';
import { FeaturesList } from '@/components/FeaturesList';
import CtaVerticalMarquee from '@/registry/default/cta-vertical-marquee/cta-vertical-marquee';
import CTAWithVerticalMarqueeLeft from '@/registry/default/cta-with-vertical-marquee-left/cta-with-vertical-marquee-left';
import CTAWithHorizontalMarquee from '@/registry/default/cta-with-horizontal-marquee/cta-with-horizontal-marquee';

<div id="vertical" />

## Vertical Marquee CTA

<ComponentPreview component={CtaVerticalMarquee} scale={0.6} />

### Usage

```tsx
import CtaVerticalMarquee from '@/registry/default/cta-vertical-marquee/cta-vertical-marquee'

export default function Page() {
  return <CtaVerticalMarquee />
}
```

<div className="mb-8" />

## Installation


<InstallationTabs 
  componentName="cta-vertical-marquee"
  componentPath="registry/default/cta-vertical-marquee/cta-vertical-marquee.tsx"
  tailwindConfig={`module.exports = {
  theme: {
    extend: {
      keyframes: {
        "marquee-vertical": {
          "0%": { transform: "translateY(0)" },
          "100%": { transform: "translateY(-100%)" }
        },
        "marquee": {
          "0%": { transform: "translateX(0)" },
          "100%": { transform: "translateX(-100%)" }
        },
        "fade-in-up": {
          "0%": { opacity: "0", transform: "translateY(20px)" },
          "100%": { opacity: "1", transform: "translateY(0)" }
        }
      },
      animation: {
        "marquee-vertical": "marquee-vertical var(--duration, 30s) linear infinite",
        "marquee": "marquee var(--duration, 40s) linear infinite",
        "fade-in-up": "fade-in-up 0.6s ease-out forwards"
      }
    }
  }
}`}
  css={`@keyframes marquee {\n  0% { transform: translateX(0); }\n  100% { transform: translateX(-100%); }\n}\n\n@keyframes marquee-vertical {\n  0% { transform: translateY(0); }\n  100% { transform: translateY(-100%); }\n}\n\n@keyframes fade-in-up {\n  0% { opacity: 0; transform: translateY(20px); }\n  100% { opacity: 1; transform: translateY(0); }\n}\n\n.animate-marquee {\n  animation: marquee var(--duration, 40s) linear infinite;\n}\n\n.animate-marquee-vertical {\n  animation: marquee-vertical var(--duration, 30s) linear infinite;\n}\n\n.animate-fade-in-up {\n  animation: fade-in-up 0.6s ease-out forwards;\n}\n\n.marquee-item { opacity: 0.25; }\n.marquee-item-horizontal { opacity: 0.25; }`}
/>

## Props

<PropsTable props={[
  { name: 'pauseOnHover', type: 'boolean', default: 'false', description: 'Pause animation on hover' },
  { name: 'reverse', type: 'boolean', default: 'false', description: 'Reverse scroll direction' },
  { name: 'speed', type: 'number', default: '30', description: 'Animation speed in seconds' },
  { name: 'className', type: 'string', default: '-', description: 'Additional CSS classes' }
]} />

## Features

<FeaturesList features={[
  { name: 'Vertical scrolling', description: 'Smooth infinite scroll animation' },
  { name: 'Opacity fade', description: 'Items fade based on distance from center' },
  { name: 'Responsive', description: 'Adapts to different screen sizes' },
  { name: 'Customizable', description: 'Speed, direction, and hover behavior' }
]} />

## Variations

<div id="left" />

### Vertical Marquee (Left Layout)

<ComponentPreview component={CTAWithVerticalMarqueeLeft} scale={0.6} />

#### Usage

```tsx
import CTAWithVerticalMarqueeLeft from '@/registry/default/cta-with-vertical-marquee-left/cta-with-vertical-marquee-left'

export default function Page() {
  return <CTAWithVerticalMarqueeLeft />
}
```

<div className="mb-8" />

<InstallationTabs 
  componentName="cta-with-vertical-marquee-left"
  componentPath="registry/default/cta-with-vertical-marquee-left/cta-with-vertical-marquee-left.tsx"
  tailwindConfig={`module.exports = {
  theme: {
    extend: {
      keyframes: {
        "marquee-vertical": {
          "0%": { transform: "translateY(0)" },
          "100%": { transform: "translateY(-100%)" }
        },
        "fade-in-up": {
          "0%": { opacity: "0", transform: "translateY(20px)" },
          "100%": { opacity: "1", transform: "translateY(0)" }
        }
      },
      animation: {
        "marquee-vertical": "marquee-vertical var(--duration, 30s) linear infinite",
        "fade-in-up": "fade-in-up 0.6s ease-out forwards"
      }
    }
  }
}`}
  css={`@keyframes marquee-vertical {\n  0% { transform: translateY(0); }\n  100% { transform: translateY(-100%); }\n}\n\n@keyframes fade-in-up {\n  0% { opacity: 0; transform: translateY(20px); }\n  100% { opacity: 1; transform: translateY(0); }\n}\n\n.animate-marquee-vertical {\n  animation: marquee-vertical var(--duration, 30s) linear infinite;\n}\n\n.animate-fade-in-up {\n  animation: fade-in-up 0.6s ease-out forwards;\n}\n\n.marquee-item { opacity: 0.25; }`}
/>


<div id="horizontal" />

### Horizontal Marquee

<ComponentPreview component={CTAWithHorizontalMarquee} scale={0.6} />

#### Usage

```tsx
import CTAWithHorizontalMarquee from '@/registry/default/cta-with-horizontal-marquee/cta-with-horizontal-marquee'

export default function Page() {
  return <CTAWithHorizontalMarquee />
}
```

<div className="mb-8" />

<InstallationTabs 
  componentName="cta-with-horizontal-marquee"
  componentPath="registry/default/cta-with-horizontal-marquee/cta-with-horizontal-marquee.tsx"
  tailwindConfig={`module.exports = {
  theme: {
    extend: {
      keyframes: {
        "marquee": {
          "0%": { transform: "translateX(0)" },
          "100%": { transform: "translateX(-100%)" }
        },
        "fade-in-up": {
          "0%": { opacity: "0", transform: "translateY(20px)" },
          "100%": { opacity: "1", transform: "translateY(0)" }
        }
      },
      animation: {
        "marquee": "marquee var(--duration, 40s) linear infinite",
        "fade-in-up": "fade-in-up 0.6s ease-out forwards"
      }
    }
  }
}`}
  css={`@keyframes marquee {\n  0% { transform: translateX(0); }\n  100% { transform: translateX(-100%); }\n}\n\n@keyframes fade-in-up {\n  0% { opacity: 0; transform: translateY(20px); }\n  100% { opacity: 1; transform: translateY(0); }\n}\n\n.animate-marquee {\n  animation: marquee var(--duration, 40s) linear infinite;\n}\n\n.animate-fade-in-up {\n  animation: fade-in-up 0.6s ease-out forwards;\n}\n\n.marquee-item-horizontal { opacity: 0.25; }`}
/>


